modded class DayZPlayerConstants
{
    // Need a dummy constant for now to avoid errors if DayZPlayerConstants is locked,
    // but typically DayZPlayerConstants allows modded values or we can just use CMD_ACTIONFB_INTERACT directly in the action class.
    // Actually we will just use standard interact in the ActionInterrogateBot class (DayZPlayerConstants.CMD_ACTIONFB_CRAFTING or similar)
}
